# 🚀 CKA: Manually Schedule Pod `tokoyo` and Expose with NodePort

In this lab, pods cannot be scheduled automatically due to special scheduling policies, so you must **manually place** the Pod onto the `controlplane` node.

Click **Start Scenario** to begin your mission! 🎯
